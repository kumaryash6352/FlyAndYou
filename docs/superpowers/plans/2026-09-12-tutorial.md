# First Flight Tutorial Implementation Plan

> Execute inline with superpowers:executing-plans; the user requested light agent use and approved this scope.

**Goal:** Deliver the approved animated tutorial with first-paint handoff and no elapsed-time loss.

**Architecture:** A small desktop tutorial state machine controls presentation and a one-shot play request. Existing world rendering, observation, neural telemetry, and edit boundaries remain authoritative.

**Tech Stack:** Rust, Bevy 0.19.1, bevy-egui 0.42.0; macOS system handwriting font with egui fallback.

**Spec:** `docs/superpowers/specs/2026-09-12-tutorial-design.md`

## Constraints

- Exact approved narration; brain and FlyCam™ appear on the simulated nervous system line.
- No model/physics steps during narration; first painting starts once after edit processing.
- Keep single-request neural scheduling and existing physical hazards.
- Work directly; no Git repository or subagents. Leave the existing game process running.

## 1. Timing and state boundaries

Files: `crates/world_core/tests/world.rs`, `crates/world_core/src/body.rs`, `crates/desktop/src/tutorial.rs`, `crates/desktop/src/main.rs`.

- [x] Add and observe failing regression: 5,000 stationary safe physics ticks must remain Running.
- [x] Add focused tutorial checks for reveal timing, indefinite final wait, early-paint rejection, and one-shot play handoff.
- [x] Remove the 4,500-tick loss and implement `Tutorial::advance`, `begin_paint`, `take_start`, and visibility state; rerun focused tests.

## 2. Native presentation and handoff

Files: `crates/desktop/src/app.rs`, `display.rs`, new `tutorial_view.rs`.

- [x] Integrate tutorial update only at a restored, paused boundary; consume the start request after normal stroke processing, before `sim.request()`.
- [x] Block play/save/restore shortcuts during narration, support skip-to-paint and explicit replay, retain normal paused drawing after completion.
- [x] Keep terrain visible from the first frame; reveal Fly and flag individually, with camera pan/zoom, stable hand lettering, rough arrows, exact caption highlights, and actual right panes at the requested line.
- [x] Keep whole-map camera and canvas geometry stable when painting clears narration; reveal the toolbox at the Paint line.

## 3. Verification and delivery

Files: `README.md`, `README.org`, `levels/01_bridge.json`, this plan.

- [x] Document startup, replay, whole-map default, and absence of a gameplay time limit.
- [x] Run `cargo fmt --all -- --check` and `cargo test --workspace --locked`.
- [x] Build and inspect a separate native instance; verify no observation before paint and the first observation includes its committed edit revision.
- [x] Rebuild the local app bundle without restarting the user's existing process; record completed checks and limits.

## Approved addition: red / yellow demonstrations

Files: `crates/world_core/src/tutorial.rs`, `examples/tutorial_frames.rs`, `controller/brainworker/tutorial.py`, `crates/desktop/src/tutorial_demo.rs`, `app.rs`, `display.rs`, `tutorial.rs`, `tutorial_view.rs`, setup/run/package scripts.

- [x] Verify both actual ray-rendered cues reach the eye without moving Fly.
- [x] Prepare 21 real anatomy samples per cue from the same full-model initial state; require observed retreat for red and approach for yellow.
- [x] Validate profile, code, atlas, world, image, and sample boundaries before showing cached responses.
- [x] Show temporary cue world, matching eye image, and recorded brain response during the two color beats; clear all demonstration state before play.
- [x] Inspect native color presentation and confirm no live play observations during demonstration; verify both cues through exact-image and full-model response checks and the untouched startup state afterward.


## Completed verification

- `cargo fmt --all -- --check` passed; all 29 workspace Rust tests passed.
- Controller Python tests: 16 passed, one opt-in full-graph socket test skipped. A separate native instance exercised the real worker instead.
- The cue generator recorded actual full-graph retreat for red and approach for yellow, 21 anatomy samples each. A second preparation run reused the validated cache.
- Native checks covered the opening terrain, Fly label and arrow, camera/map transition, exact long caption with both panes, yellow cue with corresponding colored eye image and anatomical highlights, final clearing, first-paint handoff, paused subsequent painting, and replay/reset.
- Native journal evidence: the initial play observation followed the first committed stroke at tick 0 / revision 1. Subsequent painting while paused stayed at tick 10. The final replay returned to tick 0 with no subsequent live observations and no unavailable-recording errors.
- The packaged executable matches the verified build. Only the separate test process was stopped; the user's original app and worker were preserved.
